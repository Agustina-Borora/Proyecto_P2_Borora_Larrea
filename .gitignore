# Build de NetBeans/Ant (se regenera con "ant clean jar")
/build/
/dist/

# Archivos locales de NetBeans que no conviene versionar
nbproject/private/
nbproject/Makefile-*.mk
nbproject/Package-*.bash

# Compilados sueltos
*.class

# Credenciales SMTP para el envío de mails (recuperar contraseña).
# email.properties.example SÍ se versiona, como plantilla.
email.properties

# Sistema
Thumbs.db
.DS_Store
